import React from 'react';

const Icon = () => {
    return (
        <div>
            
        </div>
    );
};

export const Veg = () => {
    return (<i className="icon veg"></i>);
}

export default Icon;